# Decision Log

This is the project's institutional memory. Every significant decision is recorded here.

| ID | Phase | Title | Decision | Context | Alternatives Considered | Consequences |
|----|-------|-------|----------|---------|------------------------|--------------|
| DL-001 | 0 | Pipeline Initiated | Start GMPDE pipeline for browser-based interactive game | User submitted idea for a simple browser-based game with interactive UI | N/A | Full 8-phase pipeline will execute; Phases 0-4 before freeze gate |
| DL-002 | 0 | Zero-dependency constraint | Pure HTML/CSS/JS with no frameworks or build tools | Simplicity is the primary goal; user wants "simple" and "browser-based" | Using a framework (React, Phaser.js) | Limits complexity but ensures instant open-and-play; no build step needed |
| DL-003 | 0 | Client-side only | No backend server; entire game runs in the browser | "Simple" implies no infrastructure; open-and-play experience | Adding a Node.js server | No persistence, no multiplayer, but zero deployment friction |
| DL-004 | 0 | Game genre deferred to architect | Specific game type to be selected in Phase 2 | User did not specify genre; architect has best context for scope/complexity tradeoffs | Asking user to pick now | Architect can optimize for demonstrability and implementation feasibility |
| DL-005 | 1 | Snake recommended as top candidate | Research recommends Snake game; Breakout as runner-up | Lowest complexity, universally known, real-time gameplay, canvas-based | 2048 (turn-based, DOM), Minesweeper (moderate learning curve), Breakout (slightly more complex) | Architect will make final genre call informed by this recommendation |
| DL-006 | 1 | Canvas API for rendering | HTML5 Canvas 2D context recommended over DOM manipulation | Real-time game loop with requestAnimationFrame suits canvas; better performance for animation | DOM-based rendering (viable for 2048-style) | Requires drawing code but enables smooth real-time graphics |
| DL-007 | 2 | Snake confirmed as game | Architect confirms Snake game selection | Best complexity/richness ratio per research; real-time loop showcases interactivity | Breakout (more complex physics), 2048 (turn-based) | All downstream design and tasks are Snake-specific |
| DL-008 | 2 | Three-file architecture | Split into index.html, style.css, game.js in game/ directory | Separation of concerns; aids readability; tasks are parallelizable | Single HTML file (simpler but harder to maintain) | Two extra file loads (negligible for local/static hosting) |
| DL-009 | 2 | setTimeout + rAF hybrid loop | Game tick via setTimeout, rendering via requestAnimationFrame | Decouples game speed from render rate; allows speed progression | Pure rAF with frame counting (harder to tune speed) | Smooth visuals independent of game tick rate |
| DL-010 | 2 | Speed progression system | Snake speeds up at score thresholds (150ms -> 90ms) | Increasing difficulty makes high scores feel earned | Constant speed (boring over time) | Requires careful tuning; 4 speed tiers defined |
| DL-011 | 3 | Seven-task decomposition | Split implementation into 7 atomic tasks (T-001 through T-007) | Each task maps to a distinct concern and is independently testable | Fewer larger tasks (less granular), more tasks (over-fragmented) | Clear dependency chain; parallel work possible on T-001/T-002 |
| DL-012 | 3 | Sequential dependency chain | Tasks follow HTML -> CSS -> State -> Input -> Loop -> Render -> Overlay order | Each layer depends on the previous; matches natural build-up of a game | All-at-once implementation (riskier, harder to debug) | Incremental verification possible at each step |
| DL-013 | 4 | All Phase 0-3 artifacts pass review | doc-reviewer approved all 4 artifacts with no blocking issues | Cross-document consistency verified; traceability matrix confirms full ADR coverage | N/A | Pipeline cleared for freeze gate; awaiting user approval to proceed to implementation |
| DL-014 | 5 | Added `started` state flag | Added boolean `started` field to state object (not in original ADR) | Needed to control start overlay visibility and prevent game loop from running before first input | Using a separate global variable | Keeps all game state in one object; minimal deviation from ADR |
| DL-015 | 5 | Added pause overlay to HTML | Added pause overlay div beyond ADR section 4 spec | ADR section 3.5 specifies Space-to-pause but section 4 only listed start/game-over overlays -- pause needs visual feedback | No pause overlay (confusing UX) | One extra DOM element; consistent UX pattern |
| DL-016 | 5 | Set-based spawnFood optimization | Changed spawnFood from O(n) array.some to O(1) Set lookup | Prevents degenerate performance when snake is long (late game) | Keep original O(n) implementation (OK for small grids) | Slightly more memory but guarantees consistent food spawn time |
| DL-017 | 5 | Batched grid line rendering | Combined individual grid line strokes into single batched path | Reduces canvas draw calls from ~70 individual strokes to 1 | Individual begin/stroke per line (original) | Measurable rendering improvement on each frame |
| DL-018 | 6 | Browser-based test runner | Created self-contained tests.html with 40 inline tests instead of Node.js test framework | game.js depends on DOM (canvas, overlays); browser-based runner is the only zero-dep option | Node.js + jsdom (adds dependency), Playwright (heavy) | Tests run by opening tests.html in any browser; no npm install needed |
| DL-019 | 6 | QA passed with no issues | All success criteria met, all ADR requirements verified, all edge cases reviewed | Comprehensive checklist validated against problem-statement.md and adr.md | N/A | Pipeline cleared for Phase 7 deployment |
| DL-020 | 7 | Local deployment verified | Game directory is self-contained, all checks pass, zero external dependencies | 4 files totaling 812 lines / 25.4 KB; file:// protocol compatible | N/A | User can open game/index.html to play immediately |
| DL-021 | 0 | v1.1 Pipeline: Realistic Snake Rendering | Enhance snake rendering from simple rectangles to anatomical (head/eyes/tongue, scaled body, tapered tail) | Current rendering is flat; user wants realistic appearance | Adding sprite images (heavier, not pure canvas), 3D/WebGL (overkill) | Only game/game.js rendering code changes; no game logic, state, or input changes |
| DL-022 | 0 | v1.1 Canvas-only rendering constraint | All new rendering uses Canvas 2D API only -- no sprites, images, or WebGL | Maintains zero-dependency constraint from v1.0; consistent with existing codebase | Loading sprite sheets (adds asset files), SVG overlays (mixing paradigms) | All visual detail must be achieved through path drawing, gradients, and arcs |
| DL-023 | 1 | Circular body segments over rectangles | Use overlapping circles (ctx.arc) instead of rounded rectangles for body segments | Overlapping circles create seamless connected body without visible gaps between segments | Keep rounded rects with connecting fills (more complex path math) | Simpler code; natural tube-like appearance; slight overlap is invisible |
| DL-024 | 1 | Radial gradient per segment for 3D effect | Use ctx.createRadialGradient() per body segment for a rounded-tube illusion | Cheap enough for <50 segments; visually effective 3D shading | Flat color (current, looks 2D), pre-rendered offscreen canvas (over-engineered) | ~0.5-1ms total overhead per frame; well within 60fps budget |
| DL-025 | 1 | Frame-counter tongue animation | Tongue flicker via module-level frame counter with modulo, no state changes | Avoids adding fields to state object; tongue is purely visual | Adding tongueOut boolean to state (violates scope), CSS animation (not applicable) | Simple implementation; deterministic flicker timing |
| DL-026 | 1 | translate/rotate for directional drawing | Use ctx.save/translate/rotate/restore pattern for head and tail orientation | Standard Canvas 2D technique; direction angle trivially derived from state.direction | Pre-computing rotated paths (complex, error-prone) | Clean code; each draw function works in local coordinates |
| DL-027 | 2 | Three specialized draw functions | Split snake rendering into drawSnakeHead, drawSnakeBody, drawSnakeTail | Each body part has distinct visual requirements; separation of concerns | Single monolithic draw function (hard to maintain), per-segment type flag (over-engineered) | Clear responsibility per function; easy to tune independently |
| DL-028 | 2 | Back-to-front draw order | Draw snake from tail to head (reverse iteration) | Ensures head overlaps body and tongue renders on top of everything | Front-to-back with z-index (canvas has no z-index) | Natural visual layering; head is always the topmost element |
| DL-029 | 2 | tongueFrameCounter as module-level var | Use a module-level counter for tongue animation, not a state field | Tongue animation is purely visual; state object should remain game-logic-only per ADR | Adding to state (mixes concerns), CSS animation (not applicable to canvas) | Zero state changes; frame counter is incremented in draw() only |
| DL-030 | 2 | Color interpolation for body gradient | Compute per-segment color via RGB interpolation based on index/totalLength | Creates natural color progression from head (bright) to tail (dark) | Uniform color (current, flat look), alternating colors (unrealistic) | Slightly more computation per segment but visually effective |
| DL-031 | 3 | Three-task decomposition for v1.1 | T-008 (head+setup), T-009 (body), T-010 (tail) in sequential order | Each task produces a testable visual improvement; head task includes shared setup since it is the foundation | Two tasks (head+body combined, tail separate), single monolithic task | Incremental verification; placeholder rendering keeps game functional between tasks |
| DL-032 | 3 | T-008 includes placeholder body/tail | T-008 uses simple circle fills as placeholders for body and tail | Ensures game remains fully playable after T-008 before T-009/T-010 are done | Stubbing with empty functions (snake disappears) | No visual regression between tasks |
