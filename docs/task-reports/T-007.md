# Task Report: T-007

**Task:** Implement UI overlays and game flow
**Status:** COMPLETE
**File:** game/game.js (lines 315-340), game/index.html (unchanged — overlays already in T-001)

## Summary
Implemented `setup()` function that runs on page load: calls `resize()`, `init()`, and `draw()` to render initial frame behind the start overlay. Added window resize listener that re-sizes canvas and re-initializes if game has not started. Overlay show/hide is handled by adding/removing the `hidden` CSS class (defined in T-002). The start overlay is hidden on first direction input (in handleInput), game-over overlay is shown by update() on collision, and pause overlay toggles on Space.
