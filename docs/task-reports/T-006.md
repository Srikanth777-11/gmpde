# Task Report: T-006

**Task:** Implement rendering
**Status:** COMPLETE
**File:** game/game.js (lines 242-313)

## Summary
Implemented `roundRect()` helper for rounded rectangle drawing and `draw()` which clears canvas with background color, draws subtle grid lines (3% white opacity), renders food as a circle with red glow (shadowBlur=15), renders snake segments as rounded rectangles with 2px inner gap (head in darker green #00cc6a, body in #00ff88), and displays score top-left and high score top-right.
