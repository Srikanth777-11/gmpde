# Task Report: T-005

**Task:** Implement game loop and update logic
**Status:** COMPLETE
**File:** game/game.js (lines 176-240)

## Summary
Implemented `checkCollision(head)` for wall and self-collision detection, `update()` which applies buffered direction, computes new head, checks collision (triggers game over with overlay if hit), grows snake on food or pops tail on empty move, and updates score/speed. `gameLoop()` uses rAF with performance.now() elapsed-time check against `state.speed` for tick-rate control. Loop self-terminates on game over or pause.
