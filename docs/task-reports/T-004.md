# Task Report: T-004

**Task:** Implement input handling
**Status:** COMPLETE
**File:** game/game.js (lines 107-174)

## Summary
Implemented `handleInput(event)` with arrow keys + WASD mapping (both lowercase and uppercase), 180-degree reversal prevention via direction buffer (`nextDirection`), Enter-to-restart on game over, Space-to-pause/unpause, and `event.preventDefault()` on arrow keys and Space. The function also handles the initial game start on first directional key press.
