# Task Report: T-003

**Task:** Implement game state and initialization
**Status:** COMPLETE
**File:** game/game.js (lines 1-105)

## Summary
Implemented constants (GRID_SIZE=20, COLORS, SPEED_TIERS), state object per ADR 3.1 (added `started` flag for start-screen flow), `resize()` with grid-aligned canvas sizing (max 800x600), `spawnFood()` with collision avoidance loop, `getSpeed()` for tier lookup, and `init()` which resets all state and places snake at center. Added `started` field to state beyond ADR spec to control start overlay visibility.
